{
  "name": "nullecho-ext",
  "private": true,
  "type": "module",
  "description": "Marks ext/src as ES modules so Node tooling (ext/rules/validate.mjs) can import the same files the service worker does.",
  "scripts": {
    "validate": "node rules/validate.mjs",
    "test": "node --test"
  }
}
